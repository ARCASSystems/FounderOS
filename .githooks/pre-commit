#!/usr/bin/env bash
set -e
if command -v python3 >/dev/null 2>&1; then
  PY=python3
elif command -v python >/dev/null 2>&1; then
  PY=python
else
  exit 0
fi
exec "$PY" "$(git rev-parse --show-toplevel)/scripts/check-private-names.py" --staged
