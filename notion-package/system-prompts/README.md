# System Prompts
System prompts for the Claude Project that runs this package. One prompt per distinct Project. `founder-os-project-prompt.md` is the primary: it runs the routing loop (transcript in, proposed Notion writes out, approval gate, then write) against the user's Notion workspace via the Notion connector.
