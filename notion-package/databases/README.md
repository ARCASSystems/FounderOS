# Databases
Notion database schemas the package uses. One schema file per database. Template rows included. Match the shape of the main Founder OS context/ files but expressed as Notion property types.
