# Founder OS Product - gitignore

# Generated by setup wizard (user-specific)
core/identity.md
context/priorities.md
context/decisions.md
context/clients.md
context/companies.md
cadence/daily-anchors.md
cadence/weekly-commitments.md
brain/log.md
brain/flags.md
brain/patterns.md
brain/decisions-parked.md
brain/archive/
brain/.snapshot.md
brain/rants/
brain/knowledge/
brain/needs-input.md
clients/
user/
drafts/
exports/
reports/

# Stack bindings (user fills these in during setup - not shareable as-is)
# Committed as template with placeholders. User's filled version is private.

# Secrets
.env
.env.*
*.key

# Local Claude settings (per-user, not shareable)
CLAUDE.local.md
AGENTS.local.md
GEMINI.local.md
.mcp.local.json
.claude/settings.local.json
.codex/
.gemini/

# OS + editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Obsidian vault config (per-user, not shareable; Obsidian writes this when the founder-os folder is opened as a vault)
.obsidian/

# Build artifacts (none expected, just in case)
dist/
build/
node_modules/

# Python bytecode (generated when tests/ runs)
__pycache__/
*.py[cod]
*$py.class

# Audit artifacts (generated, not source-of-truth)
audit-*.md

# Internal review notes (Codex findings, verification logs, working memory)
# Kept locally for reference. Not committed to the public repo.
# Already-tracked notes (v1.7 review, v1.7 codex findings/verification) stay tracked.
notes/

# Execution plans (internal release scaffolding - Codex reads locally, never commits)
plans/

# Evaluation traces (synthetic user sessions used during development QA)
# v1.21 traces committed as product evidence of multi-archetype coverage.
# v1202/v1203 traces are intermediate working notes - not product artifacts.
traces/v1202-*.md
traces/v1203-*.md
# Internal Codex review briefs - prep scaffolding, not product artifacts.
# Contain private context (names, local paths) by design. Never commit.
traces/v122-codex-final-review-brief.md

# Agent runner scratch directory (dev tooling, not product code)
.agents/

# Operator-local private-name patterns for pre-commit hook (gitignored; template is tracked)
scripts/private-name-patterns.txt
