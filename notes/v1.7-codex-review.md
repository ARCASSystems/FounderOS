# v1.7 Codex Review

Date: 2026-05-07
Verdict: READY

## Summary

I independently reviewed the v1.7 work against the original spec and executed the three shipped workstreams. WS1, WS2, WS3, and WS5 pass. WS4 is correctly deferred because no tester feedback file exists. The only findings are non-blocking: concrete example IDs in `entry-conventions.md` may appear in future query output if rules files are scanned, and WSL Git sees line-ending churn on this Windows checkout. Windows Git shows the expected 23 tracked files plus the 4 untracked v1.7 paths, and the content gates pass against that actual diff.

## Per-workstream

### WS1 (Citations-by-ID)

PASS - all 6 brain templates use placeholder IDs in `<channel>-YYYY-MM-DD-NNN` form.

PASS - `templates/rules/entry-conventions.md` puts the ID convention before the older state rules.

PASS - `scripts/query.py` and `templates/scripts/query.py` expose `top_entry_id`. No `first_entry_id` helper exists.

PASS - `.claude/commands/dream.md` requires ID-cited digest lines and forbids invented IDs.

PASS - `skills/brain-log/SKILL.md` and `skills/knowledge-capture/SKILL.md` document the highest-counter scan and `001` start rule.

### WS2 (Progressive query)

PASS - `python scripts/query.py "outreach stalled"` returns index output and exits 0.

PASS - explicit `--mode index` output matches bare query output.

PASS - `python scripts/query.py --mode timeline --anchor CLAUDE.md` returns dated entries in chronological order and exits 0.

PASS - `python scripts/query.py --mode full --ids log-2026-05-07-999` reports `not found` and exits 0.

PASS - punctuation-only and no-argument paths print the right messages and return exit 2. The command runner displays nonzero statuses as 1, so I checked `$LASTEXITCODE` directly.

PASS - `scripts/query.py` and `templates/scripts/query.py` match by `diff -q`.

### WS3 (Observation hook)

PASS - bash hook exits 0 with `FOUNDER_OS_OBSERVATIONS` unset and writes no file.

PASS - bash hook exits 0 with `FOUNDER_OS_OBSERVATIONS=1` on this Windows shell and writes no file, deferring to PowerShell as designed.

PASS - PowerShell hook in a fake repo exits 0 with the variable unset and writes no file.

PASS - PowerShell hook in a fake repo creates `brain/observations/2026-05-07.jsonl` when opted in. First bytes are `7B 22 74 73`, not a BOM.

PASS - second PowerShell call appends a second JSONL line.

PASS - `.claude/settings.json` registers both hooks under `PostToolUse` and parses as JSON.

### WS4 (deferred - install ergonomics sweep)

PASS - `notes/v1.7-codex-findings.md`, `CHANGELOG.md`, and `ROADMAP.md` all document WS4 as deferred pending tester feedback.

### WS5 (release plumbing)

PASS - `VERSION`, `.claude-plugin/plugin.json`, and `.claude-plugin/marketplace.json` all read `1.7.0`.

PASS - `CHANGELOG.md` top entry is `v1.7.0 - 2026-05-07`.

PASS - `README.md` status block and product table reference v1.7.0.

PASS - `ROADMAP.md` moves v1.7 to Shipped and lists four v1.8 candidates.

PASS - skill count is 37 by directory count, and `skills/index.md` says 37 on lines 3 and 73.

PASS - static checks passed: bash syntax for all three hooks, Python AST parse for both query scripts, JSON parse for settings, plugin, and marketplace files.

PASS - anti-leak and style gates pass against the Windows Git diff and untracked v1.7 files.

## Issues found

| Severity | File:line | Problem | Suggested fix |
|---|---|---|---|
| Low | `templates/rules/entry-conventions.md:6` | Concrete example IDs may be picked up by `top_entry_id` if rules files are scanned in a user install. This is already noted in the v1.7 build findings. | In a later patch, replace concrete examples with placeholders or skip rules files when extracting a top entry ID. |
| Info | local checkout | WSL Git sees line-ending churn across many older files and produces false leak and style hits. Windows Git shows the expected v1.7 diff. | Use Windows Git for release gates on this checkout, or add repo line-ending policy before relying on WSL Git output. |

## Things the prior review may have missed

The PowerShell BOM fix holds in a real create-and-append test. The first four bytes are correct and line count moves from 1 to 2 after the second call.

The hook registration shape in `.claude/settings.json` matches the existing `SessionStart` and `Stop` hook pattern.

No hook race was found in normal use. Both hooks are registered, but the bash hook exits on Windows-style shells so only the PowerShell hook writes there.

The README, CHANGELOG, ROADMAP, VERSION, plugin manifest, marketplace metadata, and skill index are in sync at v1.7.0.
