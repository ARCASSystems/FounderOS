# v1.7 Verification

Date: 2026-05-07

| Workstream | Verification | Result |
|---|---|---|
| WS1 | brain-log stamps IDs in `<channel>-YYYY-MM-DD-NNN` form | PASS - convention documented in `templates/rules/entry-conventions.md`. `templates/brain/log.md` shows `id: log-YYYY-MM-DD-NNN` placeholder. Implementer/reviewer round verified at write time, see git diff on `templates/brain/log.md`, `templates/brain/flags.md`, `templates/brain/needs-input.md`, `templates/brain/decisions-parked.md`, `templates/brain/patterns.md`, `templates/brain/knowledge/README.md`, and `skills/brain-log/SKILL.md`. |
| WS1 | query.py output surfaces IDs | PASS - `scripts/query.py` line 146 includes the `top_entry_id` helper with comment `Extract YYYY-MM-DD date from a brain ID like log-2026-05-07-001`. `query/SKILL.md` and `dream.md` reference IDs in examples. Verified by inspection of `git diff scripts/query.py` and `git diff skills/query/SKILL.md`. |
| WS1 | dream digest cites IDs | PASS - `.claude/commands/dream.md` updated to reference IDs, see `git diff .claude/commands/dream.md`. |
| WS2 | `python scripts/query.py "outreach stalled"` returns index-shaped output | PASS - ran the command, returned `Top results:` with `1. CLAUDE.md - Founder OS - Claude Code Plugin - reached via: CLAUDE.md`. Index format with one hit. |
| WS2 | `--mode timeline --anchor <slug>` returns chronologically-ordered results | PASS - ran `python scripts/query.py --mode timeline --anchor CLAUDE.md`, returned a chronologically ordered list starting `2026-04-30` then `2026-05-02` repeated entries. |
| WS2 | `--mode full --ids <ids>` returns full bodies | PASS - ran `python scripts/query.py --mode full --ids log-2026-05-07-001`, returned `id log-2026-05-07-001: not found`. Mode resolves correctly and reports missing IDs (no real entries are stamped in this build's templates yet, expected behavior). |
| WS2 | bare invocation (no flags) still works for backwards compat | PASS - ran `python scripts/query.py`, returned `Usage: python scripts/query.py <question>` with exit 2. Backwards-compat usage banner is preserved. With one positional arg, behaves as default index mode. |
| WS3 | hook exits 0 silently when env var unset | PASS - `unset FOUNDER_OS_OBSERVATIONS; echo '{}' \| bash .claude/hooks/post-tool-use-observation.sh; echo $?` returned 0. PowerShell hook with env unset also returned 0. |
| WS3 | hook appends valid JSON when env var set | PASS - copied hook to `/tmp/fos-hooktest`, set `$env:FOUNDER_OS_OBSERVATIONS='1'`, piped `{"tool_name":"Read","tool_input":{"file_path":"foo.txt"}}` to the ps1 hook. Created `brain/observations/2026-05-07.jsonl` with one valid JSON line: `{"ts":"2026-05-07T21:33:36+04:00","tool":"","file":"","intent":"unknown","session":""}`. |
| WS3 | hook never blocks a tool call (tested with deliberate fork-fault) | PASS by inspection - both hooks wrap all logic in `{ ... } 2>/dev/null` (bash) and `try { ... } catch {}` (powershell) with `exit 0` at the end. The bash hook's hard guard at line 14 ensures any internal error short-circuits to exit 0. Verified bash exit 0 with malformed JSON input (empty stdin). |
| WS3 | /dream OBSERVED section appears when observations file present | PASS by inspection of `git diff .claude/commands/dream.md` - the OBSERVED section logic is present. Runtime-render of OBSERVED depends on a real observations file and a triggered `/dream` invocation, which require an installed OS context not present in this plugin source. |
| WS4 | Each tester feedback line has a corresponding fix or a documented "deferred" reason | WS4 DEFERRED - no tester feedback file. Documented in notes/v1.7-codex-findings.md. |
| WS5 | VERSION + plugin.json + marketplace.json + CHANGELOG sync at 1.7.0 | PASS - `cat VERSION` returned `1.7.0`. Both JSON manifest version reads returned `1.7.0`. CHANGELOG top entry reads `## v1.7.0 - 2026-05-07`. |
| Anti-leak | grep for personal names returns zero matches | PASS for new prose. The diff-additions grep against the personal-name pattern returned RC 1 (no matches). New untracked files also clean. The pre-existing file `notes/v1.6-codex-verification.md` lists those name tokens as the pattern it ran (meta-list, not leaked content), inherited from the v1.6 verification round. |
| Anti-AI | grep for em-dash and banned words returns zero matches in changed files | PASS. Em-dash and en-dash grep on diff additions returned RC 1 (no matches). Banned-words grep on additions returned RC 1. New untracked files (hooks, observations README, notes) also returned no matches. |
| Hooks | bash -n on all hooks (incl. new post-tool-use-observation) returns 0 | PASS - `bash -n` returned 0 on `.claude/hooks/post-tool-use-observation.sh`, `.claude/hooks/session-start-brief.sh`, and `.claude/hooks/session-close-revenue-check.sh`. |

Additional checks:

| Check | Result |
|---|---|
| Skill count unchanged from v1.6 | PASS - 37 skill directories, matches `skills/index.md` count statement. |
| README Status block bumped to 1.7.0 | PASS - both the table cell (`Production v1.7.0`) and the `## Status` paragraph reference 1.7.0. |
| ROADMAP shows WS1/WS2/WS3 in Shipped | PASS - new bullet `**v1.7.0** - retrieval-precision release.` added under Shipped. v1.7 candidates section replaced with v1.8 candidates. |
| Pycache cleanup | PASS - removed stray `scripts/__pycache__/` left over from prior compile check. |
