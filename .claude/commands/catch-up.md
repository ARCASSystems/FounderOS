---
description: Sweep everything captured away from the laptop into the brain. Say "catch up" or "process my inbox" (or run /founder-os:catch-up). Files capture/inbox/ drops, pasted notes, and connected meeting-notes transcripts into brain/rants/ with provenance, runs the names-glossary correction pass, then offers /dream to distil.
---

# Catch up

Run the `catch-up` skill (`skills/catch-up/SKILL.md`) exactly as written: sweep `capture/inbox/`, any pasted text, and a connected meeting-notes tool into `brain/rants/` with `processed: false`, apply the name pass against `context/names.md`, move swept files to `capture/inbox/.processed/`, report one line per capture, and offer /dream.

If `$ARGUMENTS` contains pasted text, treat it as the capture to file.
