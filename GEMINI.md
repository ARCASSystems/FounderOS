# FounderOS - Gemini CLI entry point

Read `CLAUDE.md` first. Treat it as the source of truth.

This repo is a Claude Code plugin. If you are running in Gemini CLI or another agent surface that cannot execute Claude Code slash commands, treat `.claude/commands/` as procedural reference only. Do not claim setup, update, hooks, MCP auth, local writes, or scheduled routines have run unless the current environment actually supports them.

If founder context files are missing, stop and point the user to `/founder-os:setup` in Claude Code or the Notion quickstart once the public template is available. Do not invent identity, priorities, clients, revenue, or past decisions.
